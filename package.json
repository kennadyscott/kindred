{
  "name": "kindred-app",
  "version": "1.0.0",
  "private": true,
  "description": "Kindred — Therapy, matched to you. Native shell around the web app.",
  "scripts": {
    "build": "node build-native.js",
    "ios": "npm run build && npx cap sync ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
